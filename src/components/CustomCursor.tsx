'use client';

// CustomCursor removed — using native cursor for a cleaner, faster experience.
// This stub exists to prevent Turbopack HMR cache errors from stale references.
export default function CustomCursor() {
    return null;
}
